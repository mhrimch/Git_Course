0 verbose cli C:\Projekte\pzw-mkp\.node\node.exe C:\Projekte\pzw-mkp\.node\node_modules\npm\bin\npm-cli.js
1 info using npm@11.6.2
2 info using node@v24.13.0
3 silly config load:file:C:\Projekte\pzw-mkp\.node\node_modules\npm\npmrc
4 silly config load:file:C:\Projekte\pzw-mkp\pzw-cpl-web\src\main\typescript\zeitwirtschaft\.npmrc
5 silly config load:file:C:\Users\j780800\.npmrc
6 silly config load:file:C:\Projekte\pzw-mkp\.node\etc\npmrc
7 verbose title npm exec @finanzinformatik/cpl-scripts@latest env install --skip-install --skip-vcs
8 verbose argv "exec" "--yes" "--cache" "../../../../../.npx" "--registry" "https://nexus.intern/repository/npm-group-mkp/" "--" "@finanzinformatik/cpl-scripts@latest" "env" "install" "--skip-install" "--skip-vcs"
9 verbose logfile logs-max:10 dir:C:\Projekte\pzw-mkp\.npx\_logs\2026-05-20T14_15_09_031Z-
10 verbose logfile C:\Projekte\pzw-mkp\.npx\_logs\2026-05-20T14_15_09_031Z-debug-0.log
11 silly logfile start cleaning logs, removing 1 files
12 silly logfile done cleaning log files
13 silly packumentCache heap:4496293888 maxSize:1124073472 maxEntrySize:562036736
14 http fetch GET 200 https://nexus.intern/repository/npm-group-mkp/@finanzinformatik%2fcpl-scripts 2112ms (cache updated)
15 silly packumentCache heap:4496293888 maxSize:1124073472 maxEntrySize:562036736
16 silly packumentCache heap:4496293888 maxSize:1124073472 maxEntrySize:562036736
17 silly idealTree buildDeps
18 silly fetch manifest @finanzinformatik/cpl-scripts@9.0.2-261-PR-2134-SNAPSHOT-1
19 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@finanzinformatik%2fcpl-scripts cache-miss
20 http fetch GET 200 https://nexus.intern/repository/npm-group-mkp/@finanzinformatik%2fcpl-scripts 1904ms (cache updated)
21 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@finanzinformatik%2fcpl-scripts set size:undefined disposed:false
22 silly fetch manifest @finanzinformatik/cpl-module-templates@^5.0.3
23 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@finanzinformatik%2fcpl-module-templates cache-miss
24 http cache https://nexus.intern/repository/npm-group-mkp/@finanzinformatik%2fcpl-module-templates 12ms (cache hit)
25 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@finanzinformatik%2fcpl-module-templates set size:884353 disposed:false
26 silly fetch manifest typescript@5.9.3
27 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/typescript cache-miss
28 http cache https://nexus.intern/repository/npm-group-mkp/typescript 32ms (cache hit)
29 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/typescript set size:15478215 disposed:false
30 silly placeDep ROOT @finanzinformatik/cpl-scripts@9.0.2-261-PR-2134-SNAPSHOT-1 REPLACE for:  want: 9.0.2-261-PR-2134-SNAPSHOT-1
31 silly placeDep ROOT @finanzinformatik/cpl-module-templates@5.0.3 OK for: @finanzinformatik/cpl-scripts@9.0.2-261-PR-2134-SNAPSHOT-1 want: ^5.0.3
32 silly placeDep ROOT typescript@5.9.3 REPLACE for: @finanzinformatik/cpl-scripts@9.0.2-261-PR-2134-SNAPSHOT-1 want: 5.9.3
33 silly fetch manifest @finanzinformatik/cpl-module-vite-plugin@^5.1.0-261-PR-2134-SNAPSHOT-1
34 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@finanzinformatik%2fcpl-module-vite-plugin cache-miss
35 silly fetch manifest @finanzinformatik/eslint-plugin-fiqa-eslint@260
36 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@finanzinformatik%2feslint-plugin-fiqa-eslint cache-miss
37 silly fetch manifest figures@2
38 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/figures cache-miss
39 silly fetch manifest listr2@^10.1.0
40 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/listr2 cache-miss
41 silly fetch manifest rxjs@6.6.7
42 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/rxjs cache-miss
43 silly fetch manifest uuid@^14.0.0
44 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/uuid cache-miss
45 silly fetch manifest velocityjs@^2.1.6
46 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/velocityjs cache-miss
47 silly fetch manifest vite@^7.3.2
48 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/vite cache-miss
49 silly fetch manifest yaml@^2.8.3
50 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/yaml cache-miss
51 http cache https://nexus.intern/repository/npm-group-mkp/@finanzinformatik%2feslint-plugin-fiqa-eslint 26ms (cache hit)
52 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@finanzinformatik%2feslint-plugin-fiqa-eslint set size:249331 disposed:false
53 http cache https://nexus.intern/repository/npm-group-mkp/yaml 27ms (cache hit)
54 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/yaml set size:387665 disposed:false
55 http cache https://nexus.intern/repository/npm-group-mkp/uuid 32ms (cache hit)
56 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/uuid set size:235661 disposed:false
57 http cache https://nexus.intern/repository/npm-group-mkp/listr2 35ms (cache hit)
58 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/listr2 set size:1061351 disposed:false
59 http cache https://nexus.intern/repository/npm-group-mkp/figures 41ms (cache hit)
60 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/figures set size:65027 disposed:false
61 http cache https://nexus.intern/repository/npm-group-mkp/velocityjs 41ms (cache hit)
62 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/velocityjs set size:163156 disposed:false
63 http cache https://nexus.intern/repository/npm-group-mkp/rxjs 44ms (cache hit)
64 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/rxjs set size:983205 disposed:false
65 http cache https://nexus.intern/repository/npm-group-mkp/vite 101ms (cache hit)
66 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/vite set size:38851944 disposed:false
67 http fetch GET 200 https://nexus.intern/repository/npm-group-mkp/@finanzinformatik%2fcpl-module-vite-plugin 1208ms (cache updated)
68 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@finanzinformatik%2fcpl-module-vite-plugin set size:undefined disposed:false
69 silly fetch manifest @types/node@^20.19.0 || >=22.12.0
70 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@types%2fnode cache-miss
71 http cache https://nexus.intern/repository/npm-group-mkp/@types%2fnode 34ms (cache hit)
72 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@types%2fnode set size:11060089 disposed:false
73 silly fetch manifest jiti@>=1.21.0
74 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/jiti cache-miss
75 http cache https://nexus.intern/repository/npm-group-mkp/jiti 16ms (cache hit)
76 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/jiti set size:389986 disposed:false
77 silly fetch manifest less@^4.0.0
78 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/less cache-miss
79 http cache https://nexus.intern/repository/npm-group-mkp/less 26ms (cache hit)
80 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/less set size:464355 disposed:false
81 silly fetch manifest lightningcss@^1.21.0
82 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/lightningcss cache-miss
83 http cache https://nexus.intern/repository/npm-group-mkp/lightningcss 8ms (cache hit)
84 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/lightningcss set size:187506 disposed:false
85 silly fetch manifest sass@^1.70.0
86 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/sass cache-miss
87 http cache https://nexus.intern/repository/npm-group-mkp/sass 9ms (cache hit)
88 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/sass set size:732145 disposed:false
89 silly fetch manifest sass-embedded@^1.70.0
90 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/sass-embedded cache-miss
91 http cache https://nexus.intern/repository/npm-group-mkp/sass-embedded 10ms (cache hit)
92 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/sass-embedded set size:417657 disposed:false
93 silly fetch manifest stylus@>=0.54.8
94 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/stylus cache-miss
95 http cache https://nexus.intern/repository/npm-group-mkp/stylus 11ms (cache hit)
96 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/stylus set size:290197 disposed:false
97 silly fetch manifest sugarss@^5.0.0
98 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/sugarss cache-miss
99 http cache https://nexus.intern/repository/npm-group-mkp/sugarss 9ms (cache hit)
100 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/sugarss set size:45310 disposed:false
101 silly fetch manifest postcss@^8.3.3
102 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/postcss cache-miss
103 http cache https://nexus.intern/repository/npm-group-mkp/postcss 9ms (cache hit)
104 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/postcss set size:728571 disposed:false
105 silly fetch manifest terser@^5.16.0
106 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/terser cache-miss
107 http cache https://nexus.intern/repository/npm-group-mkp/terser 8ms (cache hit)
108 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/terser set size:804060 disposed:false
109 silly fetch manifest tsx@^4.8.1
110 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/tsx cache-miss
111 http cache https://nexus.intern/repository/npm-group-mkp/tsx 9ms (cache hit)
112 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/tsx set size:304468 disposed:false
113 silly placeDep ROOT @finanzinformatik/cpl-module-vite-plugin@5.1.0-261-PR-2134-SNAPSHOT-1 OK for: @finanzinformatik/cpl-scripts@9.0.2-261-PR-2134-SNAPSHOT-1 want: ^5.1.0-261-PR-2134-SNAPSHOT-1
114 silly placeDep ROOT vite@7.3.3 OK for: @finanzinformatik/cpl-module-vite-plugin@5.1.0-261-PR-2134-SNAPSHOT-1 want: ^7.3.2
115 silly placeDep ROOT @finanzinformatik/eslint-plugin-fiqa-eslint@260.1.0 OK for: @finanzinformatik/cpl-scripts@9.0.2-261-PR-2134-SNAPSHOT-1 want: 260
116 silly placeDep ROOT figures@2.0.0 OK for: @finanzinformatik/cpl-scripts@9.0.2-261-PR-2134-SNAPSHOT-1 want: 2
117 silly placeDep ROOT listr2@10.2.1 OK for: @finanzinformatik/cpl-scripts@9.0.2-261-PR-2134-SNAPSHOT-1 want: ^10.1.0
118 silly placeDep ROOT rxjs@6.6.7 OK for: @finanzinformatik/cpl-scripts@9.0.2-261-PR-2134-SNAPSHOT-1 want: 6.6.7
119 silly placeDep ROOT uuid@14.0.0 OK for: @finanzinformatik/cpl-scripts@9.0.2-261-PR-2134-SNAPSHOT-1 want: ^14.0.0
120 silly placeDep ROOT velocityjs@2.1.6 OK for: @finanzinformatik/cpl-scripts@9.0.2-261-PR-2134-SNAPSHOT-1 want: ^2.1.6
121 silly placeDep ROOT yaml@2.9.0 REPLACE for: @finanzinformatik/cpl-scripts@9.0.2-261-PR-2134-SNAPSHOT-1 want: ^2.8.3
122 silly fetch manifest node-fetch-commonjs@^3.3.1
123 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/node-fetch-commonjs cache-miss
124 silly fetch manifest vite-plugin-externals@^0.6.2
125 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/vite-plugin-externals cache-miss
126 silly fetch manifest fdir@^6.5.0
127 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/fdir cache-miss
128 silly fetch manifest rollup@^4.43.0
129 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/rollup cache-miss
130 silly fetch manifest esbuild@^0.27.0
131 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/esbuild cache-miss
132 silly fetch manifest postcss@^8.5.6
133 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/postcss cache-hit
134 silly fetch manifest picomatch@^4.0.3
135 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/picomatch cache-miss
136 silly fetch manifest tinyglobby@^0.2.15
137 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/tinyglobby cache-miss
138 silly fetch manifest fsevents@~2.3.3
139 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/fsevents cache-miss
140 silly fetch manifest eslint@8.57.0
141 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/eslint cache-miss
142 silly fetch manifest eslint-plugin-react@7.35.0
143 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/eslint-plugin-react cache-miss
144 silly fetch manifest eslint-plugin-react-hooks@4.6.2
145 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/eslint-plugin-react-hooks cache-miss
146 silly fetch manifest @typescript-eslint/eslint-plugin@6.21.0
147 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2feslint-plugin cache-miss
148 silly fetch manifest @typescript-eslint/parser@6.21.0
149 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2fparser cache-miss
150 silly fetch manifest typescript@5.9.2
151 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/typescript cache-miss
152 silly fetch manifest eslint-plugin-import@2.29.1
153 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/eslint-plugin-import cache-miss
154 http cache https://nexus.intern/repository/npm-group-mkp/tinyglobby 37ms (cache hit)
155 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/tinyglobby set size:52995 disposed:false
156 silly fetch manifest escape-string-regexp@^1.0.5
157 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/escape-string-regexp cache-miss
158 http cache https://nexus.intern/repository/npm-group-mkp/node-fetch-commonjs 43ms (cache hit)
159 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/node-fetch-commonjs set size:21015 disposed:false
160 silly fetch manifest cli-truncate@^5.2.0
161 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/cli-truncate cache-miss
162 http cache https://nexus.intern/repository/npm-group-mkp/vite-plugin-externals 46ms (cache hit)
163 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/vite-plugin-externals set size:68745 disposed:false
164 silly fetch manifest eventemitter3@^5.0.4
165 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/eventemitter3 cache-miss
166 http cache https://nexus.intern/repository/npm-group-mkp/fdir 50ms (cache hit)
167 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/fdir set size:137056 disposed:false
168 silly fetch manifest log-update@^6.1.0
169 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/log-update cache-miss
170 http cache https://nexus.intern/repository/npm-group-mkp/picomatch 55ms (cache hit)
171 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/picomatch set size:124144 disposed:false
172 silly fetch manifest rfdc@^1.4.1
173 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/rfdc cache-miss
174 http cache https://nexus.intern/repository/npm-group-mkp/esbuild 60ms (cache hit)
175 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/esbuild set size:1263782 disposed:false
176 silly fetch manifest wrap-ansi@^10.0.0
177 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/wrap-ansi cache-miss
178 http cache https://nexus.intern/repository/npm-group-mkp/rollup 83ms (cache hit)
179 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/rollup set size:6211406 disposed:false
180 silly fetch manifest tslib@^1.9.0
181 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/tslib cache-miss
182 http cache https://nexus.intern/repository/npm-group-mkp/eslint-plugin-react-hooks 151ms (cache hit)
183 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/eslint-plugin-react-hooks set size:7421219 disposed:false
184 silly fetch manifest debug@^4.3.4
185 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/debug cache-miss
186 http cache https://nexus.intern/repository/npm-group-mkp/eslint-plugin-react 178ms (cache hit)
187 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/eslint-plugin-react set size:648768 disposed:false
188 http cache https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2fparser 206ms (cache hit)
189 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2fparser set size:15277166 disposed:false
190 http cache https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2feslint-plugin 290ms (cache hit)
191 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2feslint-plugin set size:18016344 disposed:false
192 http cache https://nexus.intern/repository/npm-group-mkp/typescript 362ms (cache hit)
193 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/typescript set size:15478215 disposed:false
194 http cache https://nexus.intern/repository/npm-group-mkp/fsevents 470ms (cache hit)
195 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/fsevents set size:183699 disposed:false
196 http cache https://nexus.intern/repository/npm-group-mkp/eslint-plugin-import 472ms (cache hit)
197 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/eslint-plugin-import set size:487702 disposed:false
198 http cache https://nexus.intern/repository/npm-group-mkp/eslint 491ms (cache hit)
199 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/eslint set size:2002446 disposed:false
200 http cache https://nexus.intern/repository/npm-group-mkp/wrap-ansi 493ms (cache hit)
201 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/wrap-ansi set size:55672 disposed:false
202 http cache https://nexus.intern/repository/npm-group-mkp/escape-string-regexp 526ms (cache hit)
203 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/escape-string-regexp set size:21384 disposed:false
204 http cache https://nexus.intern/repository/npm-group-mkp/rfdc 511ms (cache hit)
205 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/rfdc set size:45603 disposed:false
206 http cache https://nexus.intern/repository/npm-group-mkp/log-update 515ms (cache hit)
207 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/log-update set size:61143 disposed:false
208 http cache https://nexus.intern/repository/npm-group-mkp/cli-truncate 527ms (cache hit)
209 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/cli-truncate set size:35417 disposed:false
210 http cache https://nexus.intern/repository/npm-group-mkp/tslib 432ms (cache hit)
211 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/tslib set size:107010 disposed:false
212 http cache https://nexus.intern/repository/npm-group-mkp/debug 397ms (cache hit)
213 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/debug set size:194746 disposed:false
214 http cache https://nexus.intern/repository/npm-group-mkp/eventemitter3 531ms (cache hit)
215 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/eventemitter3 set size:92968 disposed:false
216 silly fetch manifest yaml@^2.4.2
217 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/yaml cache-hit
218 silly placeDep ROOT node-fetch-commonjs@3.3.2 OK for: @finanzinformatik/cpl-module-vite-plugin@5.1.0-261-PR-2134-SNAPSHOT-1 want: ^3.3.1
219 silly placeDep ROOT vite-plugin-externals@0.6.2 OK for: @finanzinformatik/cpl-module-vite-plugin@5.1.0-261-PR-2134-SNAPSHOT-1 want: ^0.6.2
220 silly fetch manifest web-streams-polyfill@^3.0.3
221 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/web-streams-polyfill cache-miss
222 silly fetch manifest acorn@^8.4.0
223 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/acorn cache-miss
224 silly fetch manifest es-module-lexer@^0.4.1
225 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/es-module-lexer cache-miss
226 silly fetch manifest fs-extra@^10.0.0
227 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/fs-extra cache-miss
228 silly fetch manifest magic-string@^0.25.7
229 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/magic-string cache-miss
230 http cache https://nexus.intern/repository/npm-group-mkp/web-streams-polyfill 19ms (cache hit)
231 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/web-streams-polyfill set size:141483 disposed:false
232 http cache https://nexus.intern/repository/npm-group-mkp/acorn 20ms (cache hit)
233 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/acorn set size:380564 disposed:false
234 http cache https://nexus.intern/repository/npm-group-mkp/magic-string 23ms (cache hit)
235 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/magic-string set size:334127 disposed:false
236 http cache https://nexus.intern/repository/npm-group-mkp/fs-extra 27ms (cache hit)
237 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/fs-extra set size:236815 disposed:false
238 http cache https://nexus.intern/repository/npm-group-mkp/es-module-lexer 30ms (cache hit)
239 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/es-module-lexer set size:206712 disposed:false
240 silly placeDep node_modules/@finanzinformatik/eslint-plugin-fiqa-eslint @typescript-eslint/eslint-plugin@6.21.0 OK for: @finanzinformatik/eslint-plugin-fiqa-eslint@260.1.0 want: 6.21.0
241 silly placeDep node_modules/@finanzinformatik/eslint-plugin-fiqa-eslint @typescript-eslint/parser@6.21.0 OK for: @typescript-eslint/eslint-plugin@6.21.0 want: ^6.0.0 || ^6.0.0-alpha
242 silly placeDep node_modules/@finanzinformatik/eslint-plugin-fiqa-eslint eslint@8.57.0 OK for: @finanzinformatik/eslint-plugin-fiqa-eslint@260.1.0 want: 8.57.0
243 silly placeDep ROOT eslint-plugin-import@2.29.1 OK for: @finanzinformatik/eslint-plugin-fiqa-eslint@260.1.0 want: 2.29.1
244 silly placeDep node_modules/@finanzinformatik/eslint-plugin-fiqa-eslint eslint-plugin-react@7.35.0 OK for: @finanzinformatik/eslint-plugin-fiqa-eslint@260.1.0 want: 7.35.0
245 silly placeDep node_modules/@finanzinformatik/eslint-plugin-fiqa-eslint eslint-plugin-react-hooks@4.6.2 OK for: @finanzinformatik/eslint-plugin-fiqa-eslint@260.1.0 want: 4.6.2
246 silly placeDep node_modules/@finanzinformatik/eslint-plugin-fiqa-eslint typescript@5.9.2 OK for: @finanzinformatik/eslint-plugin-fiqa-eslint@260.1.0 want: 5.9.2
247 silly fetch manifest ignore@^5.2.4
248 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/ignore cache-miss
249 silly fetch manifest graphemer@^1.4.0
250 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/graphemer cache-miss
251 silly fetch manifest ts-api-utils@^1.0.1
252 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/ts-api-utils cache-miss
253 silly fetch manifest natural-compare@^1.4.0
254 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/natural-compare cache-miss
255 silly fetch manifest @typescript-eslint/utils@6.21.0
256 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2futils cache-miss
257 silly fetch manifest @eslint-community/regexpp@^4.5.1
258 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@eslint-community%2fregexpp cache-miss
259 silly fetch manifest @typescript-eslint/type-utils@6.21.0
260 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2ftype-utils cache-miss
261 silly fetch manifest @typescript-eslint/visitor-keys@6.21.0
262 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2fvisitor-keys cache-miss
263 silly fetch manifest @typescript-eslint/scope-manager@6.21.0
264 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2fscope-manager cache-miss
265 silly fetch manifest @typescript-eslint/types@6.21.0
266 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2ftypes cache-miss
267 silly fetch manifest @typescript-eslint/visitor-keys@6.21.0
268 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2fvisitor-keys cache-miss
269 silly fetch manifest @typescript-eslint/scope-manager@6.21.0
270 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2fscope-manager cache-miss
271 silly fetch manifest @typescript-eslint/typescript-estree@6.21.0
272 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2ftypescript-estree cache-miss
273 silly fetch manifest ajv@^6.12.4
274 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/ajv cache-miss
275 silly fetch manifest levn@^0.4.1
276 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/levn cache-miss
277 http cache https://nexus.intern/repository/npm-group-mkp/natural-compare 35ms (cache hit)
278 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/natural-compare set size:6556 disposed:false
279 silly fetch manifest debug@^4.3.2
280 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/debug cache-hit
281 silly fetch manifest espree@^9.6.1
282 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/espree cache-miss
283 http cache https://nexus.intern/repository/npm-group-mkp/graphemer 40ms (cache hit)
284 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/graphemer set size:26107 disposed:false
285 silly fetch manifest ignore@^5.2.0
286 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/ignore cache-miss
287 http cache https://nexus.intern/repository/npm-group-mkp/ignore 43ms (cache hit)
288 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/ignore set size:234173 disposed:false
289 silly fetch manifest esquery@^1.4.2
290 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/esquery cache-miss
291 http cache https://nexus.intern/repository/npm-group-mkp/ts-api-utils 49ms (cache hit)
292 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/ts-api-utils set size:227872 disposed:false
293 silly fetch manifest esutils@^2.0.2
294 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/esutils cache-miss
295 http cache https://nexus.intern/repository/npm-group-mkp/@eslint-community%2fregexpp 57ms (cache hit)
296 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@eslint-community%2fregexpp set size:107109 disposed:false
297 silly fetch manifest find-up@^5.0.0
298 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/find-up cache-miss
299 http cache https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2futils 83ms (cache hit)
300 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2futils set size:10205589 disposed:false
301 silly fetch manifest globals@^13.19.0
302 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/globals cache-miss
303 http cache https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2fscope-manager 170ms (cache hit)
304 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2fscope-manager set size:12509473 disposed:false
305 silly fetch manifest is-glob@^4.0.0
306 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/is-glob cache-miss
307 http cache https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2ftypes 231ms (cache hit)
308 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2ftypes set size:12322497 disposed:false
309 silly fetch manifest js-yaml@^4.1.0
310 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/js-yaml cache-miss
311 http cache https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2ftypescript-estree 308ms (cache hit)
312 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2ftypescript-estree set size:16478750 disposed:false
313 silly fetch manifest doctrine@^3.0.0
314 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/doctrine cache-miss
315 http cache https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2fvisitor-keys 387ms (cache hit)
316 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2fvisitor-keys set size:11912509 disposed:false
317 silly fetch manifest minimatch@^3.1.2
318 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/minimatch cache-miss
319 http cache https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2ftype-utils 489ms (cache hit)
320 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2ftype-utils set size:9370225 disposed:false
321 silly fetch manifest @eslint/js@8.57.0
322 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@eslint%2fjs cache-miss
323 http cache https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2fscope-manager 564ms (cache hit)
324 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2fscope-manager set size:12509473 disposed:false
325 silly fetch manifest optionator@^0.9.3
326 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/optionator cache-miss
327 http cache https://nexus.intern/repository/npm-group-mkp/levn 614ms (cache hit)
328 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/levn set size:26417 disposed:false
329 silly fetch manifest strip-ansi@^6.0.1
330 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/strip-ansi cache-miss
331 http cache https://nexus.intern/repository/npm-group-mkp/ajv 618ms (cache hit)
332 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/ajv set size:1141989 disposed:false
333 silly fetch manifest text-table@^0.2.0
334 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/text-table cache-miss
335 http cache https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2fvisitor-keys 647ms (cache hit)
336 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@typescript-eslint%2fvisitor-keys set size:11912509 disposed:false
337 silly fetch manifest cross-spawn@^7.0.2
338 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/cross-spawn cache-miss
339 http cache https://nexus.intern/repository/npm-group-mkp/esutils 658ms (cache hit)
340 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/esutils set size:27738 disposed:false
341 silly fetch manifest glob-parent@^6.0.2
342 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/glob-parent cache-miss
343 http cache https://nexus.intern/repository/npm-group-mkp/ignore 675ms (cache hit)
344 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/ignore set size:234173 disposed:false
345 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/ignore dispose
346 silly fetch manifest imurmurhash@^0.1.4
347 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/imurmurhash cache-miss
348 http cache https://nexus.intern/repository/npm-group-mkp/esquery 673ms (cache hit)
349 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/esquery set size:46643 disposed:false
350 silly fetch manifest eslint-scope@^7.2.2
351 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/eslint-scope cache-miss
352 http cache https://nexus.intern/repository/npm-group-mkp/espree 684ms (cache hit)
353 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/espree set size:325362 disposed:false
354 silly fetch manifest lodash.merge@^4.6.2
355 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/lodash.merge cache-miss
356 http cache https://nexus.intern/repository/npm-group-mkp/find-up 665ms (cache hit)
357 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/find-up set size:42879 disposed:false
358 silly fetch manifest is-path-inside@^3.0.3
359 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/is-path-inside cache-miss
360 http cache https://nexus.intern/repository/npm-group-mkp/is-glob 514ms (cache hit)
361 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/is-glob set size:38301 disposed:false
362 silly fetch manifest fast-deep-equal@^3.1.3
363 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/fast-deep-equal cache-miss
364 http cache https://nexus.intern/repository/npm-group-mkp/globals 580ms (cache hit)
365 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/globals set size:316155 disposed:false
366 silly fetch manifest @eslint/eslintrc@^2.1.4
367 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@eslint%2feslintrc cache-miss
368 http cache https://nexus.intern/repository/npm-group-mkp/js-yaml 449ms (cache hit)
369 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/js-yaml set size:151429 disposed:false
370 silly fetch manifest @nodelib/fs.walk@^1.2.8
371 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@nodelib%2ffs.walk cache-miss
372 http cache https://nexus.intern/repository/npm-group-mkp/minimatch 261ms (cache hit)
373 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/minimatch set size:376896 disposed:false
374 silly fetch manifest file-entry-cache@^6.0.1
375 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/file-entry-cache cache-miss
376 http cache https://nexus.intern/repository/npm-group-mkp/doctrine 369ms (cache hit)
377 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/doctrine set size:61276 disposed:false
378 silly fetch manifest eslint-visitor-keys@^3.4.3
379 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/eslint-visitor-keys cache-miss
380 http cache https://nexus.intern/repository/npm-group-mkp/optionator 123ms (cache hit)
381 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/optionator set size:48076 disposed:false
382 silly fetch manifest escape-string-regexp@^4.0.0
383 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/escape-string-regexp cache-hit
384 silly fetch manifest @ungap/structured-clone@^1.2.0
385 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@ungap%2fstructured-clone cache-miss
386 http cache https://nexus.intern/repository/npm-group-mkp/@eslint%2fjs 195ms (cache hit)
387 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@eslint%2fjs set size:141630 disposed:false
388 silly fetch manifest @eslint-community/regexpp@^4.6.1
389 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@eslint-community%2fregexpp cache-hit
390 silly fetch manifest @humanwhocodes/config-array@^0.11.14
391 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@humanwhocodes%2fconfig-array cache-miss
392 http cache https://nexus.intern/repository/npm-group-mkp/text-table 113ms (cache hit)
393 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/text-table set size:17334 disposed:false
394 silly fetch manifest @eslint-community/eslint-utils@^4.2.0
395 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@eslint-community%2feslint-utils cache-miss
396 http cache https://nexus.intern/repository/npm-group-mkp/strip-ansi 126ms (cache hit)
397 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/strip-ansi set size:43206 disposed:false
398 silly fetch manifest @humanwhocodes/module-importer@^1.0.1
399 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@humanwhocodes%2fmodule-importer cache-miss
400 http cache https://nexus.intern/repository/npm-group-mkp/cross-spawn 61ms (cache hit)
401 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/cross-spawn set size:107733 disposed:false
402 silly fetch manifest json-stable-stringify-without-jsonify@^1.0.1
403 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/json-stable-stringify-without-jsonify cache-miss
404 http cache https://nexus.intern/repository/npm-group-mkp/eslint-scope 53ms (cache hit)
405 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/eslint-scope set size:97018 disposed:false
406 silly fetch manifest debug@^3.2.7
407 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/debug cache-hit
408 silly fetch manifest hasown@^2.0.0
409 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/hasown cache-miss
410 http cache https://nexus.intern/repository/npm-group-mkp/imurmurhash 58ms (cache hit)
411 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/imurmurhash set size:30350 disposed:false
412 silly fetch manifest semver@^6.3.1
413 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/semver cache-miss
414 http cache https://nexus.intern/repository/npm-group-mkp/glob-parent 63ms (cache hit)
415 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/glob-parent set size:40208 disposed:false
416 silly fetch manifest is-glob@^4.0.3
417 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/is-glob cache-hit
418 silly fetch manifest doctrine@^2.1.0
419 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/doctrine cache-hit
420 silly fetch manifest object.values@^1.1.7
421 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/object.values cache-miss
422 http cache https://nexus.intern/repository/npm-group-mkp/lodash.merge 57ms (cache hit)
423 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/lodash.merge set size:79233 disposed:false
424 silly fetch manifest array-includes@^3.1.7
425 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/array-includes cache-miss
426 http cache https://nexus.intern/repository/npm-group-mkp/fast-deep-equal 56ms (cache hit)
427 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/fast-deep-equal set size:46451 disposed:false
428 silly fetch manifest is-core-module@^2.13.1
429 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/is-core-module cache-miss
430 http cache https://nexus.intern/repository/npm-group-mkp/is-path-inside 58ms (cache hit)
431 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/is-path-inside set size:23730 disposed:false
432 silly fetch manifest object.groupby@^1.0.1
433 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/object.groupby cache-miss
434 http cache https://nexus.intern/repository/npm-group-mkp/eslint-visitor-keys 47ms (cache hit)
435 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/eslint-visitor-keys set size:65493 disposed:false
436 silly fetch manifest tsconfig-paths@^3.15.0
437 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/tsconfig-paths cache-miss
438 http cache https://nexus.intern/repository/npm-group-mkp/@eslint%2feslintrc 57ms (cache hit)
439 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@eslint%2feslintrc set size:167164 disposed:false
440 silly fetch manifest object.fromentries@^2.0.7
441 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/object.fromentries cache-miss
442 http cache https://nexus.intern/repository/npm-group-mkp/@nodelib%2ffs.walk 57ms (cache hit)
443 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@nodelib%2ffs.walk set size:39665 disposed:false
444 silly fetch manifest eslint-module-utils@^2.8.0
445 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/eslint-module-utils cache-miss
446 http cache https://nexus.intern/repository/npm-group-mkp/@ungap%2fstructured-clone 52ms (cache hit)
447 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@ungap%2fstructured-clone set size:59797 disposed:false
448 silly fetch manifest array.prototype.flat@^1.3.2
449 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/array.prototype.flat cache-miss
450 http cache https://nexus.intern/repository/npm-group-mkp/file-entry-cache 56ms (cache hit)
451 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/file-entry-cache set size:151389 disposed:false
452 silly fetch manifest array.prototype.flatmap@^1.3.2
453 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/array.prototype.flatmap cache-miss
454 http cache https://nexus.intern/repository/npm-group-mkp/@humanwhocodes%2fconfig-array 54ms (cache hit)
455 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@humanwhocodes%2fconfig-array set size:140284 disposed:false
456 silly fetch manifest eslint-import-resolver-node@^0.3.9
457 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/eslint-import-resolver-node cache-miss
458 http cache https://nexus.intern/repository/npm-group-mkp/@humanwhocodes%2fmodule-importer 53ms (cache hit)
459 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@humanwhocodes%2fmodule-importer set size:9510 disposed:false
460 silly fetch manifest array.prototype.findlastindex@^1.2.3
461 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/array.prototype.findlastindex cache-miss
462 http cache https://nexus.intern/repository/npm-group-mkp/@eslint-community%2feslint-utils 55ms (cache hit)
463 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/@eslint-community%2feslint-utils set size:93731 disposed:false
464 silly fetch manifest hasown@^2.0.2
465 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/hasown cache-miss
466 http cache https://nexus.intern/repository/npm-group-mkp/json-stable-stringify-without-jsonify 55ms (cache hit)
467 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/json-stable-stringify-without-jsonify set size:4607 disposed:false
468 silly fetch manifest semver@^6.3.1
469 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/semver cache-miss
470 http cache https://nexus.intern/repository/npm-group-mkp/is-core-module 54ms (cache hit)
471 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/is-core-module set size:72135 disposed:false
472 silly fetch manifest resolve@^2.0.0-next.5
473 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/resolve cache-miss
474 http cache https://nexus.intern/repository/npm-group-mkp/hasown 64ms (cache hit)
475 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/hasown set size:15479 disposed:false
476 silly fetch manifest estraverse@^5.3.0
477 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/estraverse cache-miss
478 http cache https://nexus.intern/repository/npm-group-mkp/object.values 62ms (cache hit)
479 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/object.values set size:46238 disposed:false
480 silly fetch manifest prop-types@^15.8.1
481 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/prop-types cache-miss
482 http cache https://nexus.intern/repository/npm-group-mkp/semver 66ms (cache hit)
483 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/semver set size:232976 disposed:false
484 silly fetch manifest jsx-ast-utils@^2.4.1 || ^3.0.0
485 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/jsx-ast-utils cache-miss
486 http cache https://nexus.intern/repository/npm-group-mkp/object.groupby 63ms (cache hit)
487 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/object.groupby set size:15536 disposed:false
488 silly fetch manifest object.values@^1.2.0
489 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/object.values cache-hit
490 silly fetch manifest array-includes@^3.1.8
491 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/array-includes cache-miss
492 http cache https://nexus.intern/repository/npm-group-mkp/array-includes 67ms (cache hit)
493 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/array-includes set size:74930 disposed:false
494 silly fetch manifest object.entries@^1.1.8
495 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/object.entries cache-miss
496 http cache https://nexus.intern/repository/npm-group-mkp/tsconfig-paths 64ms (cache hit)
497 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/tsconfig-paths set size:193470 disposed:false
498 silly fetch manifest object.fromentries@^2.0.8
499 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/object.fromentries cache-miss
500 http cache https://nexus.intern/repository/npm-group-mkp/object.fromentries 65ms (cache hit)
501 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/object.fromentries set size:35248 disposed:false
502 silly fetch manifest es-iterator-helpers@^1.0.19
503 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/es-iterator-helpers cache-miss
504 http cache https://nexus.intern/repository/npm-group-mkp/eslint-module-utils 65ms (cache hit)
505 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/eslint-module-utils set size:78078 disposed:false
506 silly fetch manifest array.prototype.flatmap@^1.3.2
507 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/array.prototype.flatmap cache-miss
508 http cache https://nexus.intern/repository/npm-group-mkp/array.prototype.flat 65ms (cache hit)
509 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/array.prototype.flat set size:38453 disposed:false
510 silly fetch manifest string.prototype.repeat@^1.0.0
511 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/string.prototype.repeat cache-miss
512 http cache https://nexus.intern/repository/npm-group-mkp/array.prototype.flatmap 65ms (cache hit)
513 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/array.prototype.flatmap set size:49047 disposed:false
514 silly fetch manifest array.prototype.findlast@^1.2.5
515 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/array.prototype.findlast cache-miss
516 http cache https://nexus.intern/repository/npm-group-mkp/array.prototype.findlastindex 62ms (cache hit)
517 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/array.prototype.findlastindex set size:44973 disposed:false
518 silly fetch manifest array.prototype.tosorted@^1.1.4
519 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/array.prototype.tosorted cache-miss
520 http cache https://nexus.intern/repository/npm-group-mkp/hasown 62ms (cache hit)
521 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/hasown set size:15479 disposed:false
522 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/hasown dispose
523 silly fetch manifest string.prototype.matchall@^4.0.11
524 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/string.prototype.matchall cache-miss
525 http cache https://nexus.intern/repository/npm-group-mkp/eslint-import-resolver-node 66ms (cache hit)
526 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/eslint-import-resolver-node set size:39415 disposed:false
527 http cache https://nexus.intern/repository/npm-group-mkp/semver 64ms (cache hit)
528 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/semver set size:232976 disposed:false
529 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/semver dispose
530 http cache https://nexus.intern/repository/npm-group-mkp/resolve 46ms (cache hit)
531 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/resolve set size:243820 disposed:false
532 http cache https://nexus.intern/repository/npm-group-mkp/estraverse 48ms (cache hit)
533 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/estraverse set size:58960 disposed:false
534 http cache https://nexus.intern/repository/npm-group-mkp/prop-types 48ms (cache hit)
535 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/prop-types set size:73449 disposed:false
536 http cache https://nexus.intern/repository/npm-group-mkp/array-includes 44ms (cache hit)
537 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/array-includes set size:74930 disposed:false
538 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/array-includes dispose
539 http cache https://nexus.intern/repository/npm-group-mkp/jsx-ast-utils 48ms (cache hit)
540 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/jsx-ast-utils set size:114143 disposed:false
541 http cache https://nexus.intern/repository/npm-group-mkp/object.entries 48ms (cache hit)
542 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/object.entries set size:47337 disposed:false
543 http cache https://nexus.intern/repository/npm-group-mkp/object.fromentries 46ms (cache hit)
544 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/object.fromentries set size:35248 disposed:false
545 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/object.fromentries dispose
546 http cache https://nexus.intern/repository/npm-group-mkp/string.prototype.repeat 40ms (cache hit)
547 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/string.prototype.repeat set size:8620 disposed:false
548 http cache https://nexus.intern/repository/npm-group-mkp/es-iterator-helpers 45ms (cache hit)
549 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/es-iterator-helpers set size:258952 disposed:false
550 http cache https://nexus.intern/repository/npm-group-mkp/array.prototype.flatmap 45ms (cache hit)
551 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/array.prototype.flatmap set size:49047 disposed:false
552 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/array.prototype.flatmap dispose
553 http cache https://nexus.intern/repository/npm-group-mkp/array.prototype.findlast 44ms (cache hit)
554 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/array.prototype.findlast set size:40449 disposed:false
555 http cache https://nexus.intern/repository/npm-group-mkp/array.prototype.tosorted 43ms (cache hit)
556 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/array.prototype.tosorted set size:24731 disposed:false
557 http cache https://nexus.intern/repository/npm-group-mkp/string.prototype.matchall 43ms (cache hit)
558 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/string.prototype.matchall set size:61162 disposed:false
559 silly fetch manifest string-width@^5.1.2
560 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/string-width cache-miss
561 http cache https://nexus.intern/repository/npm-group-mkp/string-width 8ms (cache hit)
562 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/string-width set size:68671 disposed:false
563 silly fetch manifest strip-ansi@^7.0.1
564 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/strip-ansi cache-hit
565 silly fetch manifest wrap-ansi@^8.1.0
566 silly packumentCache full:https://nexus.intern/repository/npm-group-mkp/wrap-ansi cache-hit
567 silly placeDep ROOT string-width@5.1.2 OK for: @isaacs/cliui@8.0.2 want: ^5.1.2
568 verbose stack TypeError: Invalid Version: 
568 verbose stack     at new SemVer (C:\Projekte\pzw-mkp\.node\node_modules\npm\node_modules\semver\classes\semver.js:40:13)
568 verbose stack     at compare (C:\Projekte\pzw-mkp\.node\node_modules\npm\node_modules\semver\functions\compare.js:5:32)
568 verbose stack     at Object.eq (C:\Projekte\pzw-mkp\.node\node_modules\npm\node_modules\semver\functions\eq.js:4:29)
568 verbose stack     at Node.canDedupe (C:\Projekte\pzw-mkp\.node\node_modules\npm\node_modules\@npmcli\arborist\lib\node.js:1140:32)
568 verbose stack     at PlaceDep.pruneDedupable (C:\Projekte\pzw-mkp\.node\node_modules\npm\node_modules\@npmcli\arborist\lib\place-dep.js:426:14)
568 verbose stack     at new PlaceDep (C:\Projekte\pzw-mkp\.node\node_modules\npm\node_modules\@npmcli\arborist\lib\place-dep.js:278:14)
568 verbose stack     at #buildDepStep (C:\Projekte\pzw-mkp\.node\node_modules\npm\node_modules\@npmcli\arborist\lib\arborist\build-ideal-tree.js:945:18)
568 verbose stack     at async Arborist.buildIdealTree (C:\Projekte\pzw-mkp\.node\node_modules\npm\node_modules\@npmcli\arborist\lib\arborist\build-ideal-tree.js:182:7)
568 verbose stack     at async Promise.all (index 1)
568 verbose stack     at async Arborist.reify (C:\Projekte\pzw-mkp\.node\node_modules\npm\node_modules\@npmcli\arborist\lib\arborist\reify.js:114:5)
569 error Invalid Version: 
570 silly unfinished npm timer reify 1779286513464
571 silly unfinished npm timer reify:loadTrees 1779286513466
572 silly unfinished npm timer idealTree:buildDeps 1779286513791
573 silly unfinished npm timer idealTree:node_modules/@isaacs/cliui 1779286518914
574 verbose cwd C:\Projekte\pzw-mkp\pzw-cpl-web\src\main\typescript\zeitwirtschaft
575 verbose os Windows_NT 10.0.22631
576 verbose node v24.13.0
=====================================
// mixin implementing the buildIdealTree method
const localeCompare = require('@isaacs/string-locale-compare')('en')
const PackageJson = require('@npmcli/package-json')
const npa = require('npm-package-arg')
const pacote = require('pacote')
const cacache = require('cacache')
const { callLimit: promiseCallLimit } = require('promise-call-limit')
const realpath = require('../../lib/realpath.js')
const { resolve, dirname, sep } = require('node:path')
const treeCheck = require('../tree-check.js')
const { readdirScoped } = require('@npmcli/fs')
const { lstat, readlink } = require('node:fs/promises')
const { depth } = require('treeverse')
const { log, time } = require('proc-log')
const { redact } = require('@npmcli/redact')
const semver = require('semver')

const {
  OK,
  REPLACE,
  CONFLICT,
} = require('../can-place-dep.js')
const PlaceDep = require('../place-dep.js')

const debug = require('../debug.js')
const fromPath = require('../from-path.js')
const calcDepFlags = require('../calc-dep-flags.js')
const Shrinkwrap = require('../shrinkwrap.js')
const { defaultLockfileVersion } = Shrinkwrap
const Node = require('../node.js')
const Link = require('../link.js')
const addRmPkgDeps = require('../add-rm-pkg-deps.js')
const optionalSet = require('../optional-set.js')
const { checkEngine, checkPlatform } = require('npm-install-checks')
const relpath = require('../relpath.js')
const resetDepFlags = require('../reset-dep-flags.js')

// note: some of these symbols are shared so we can hit
// them with unit tests and reuse them across mixins
const _updateAll = Symbol.for('updateAll')
const _flagsSuspect = Symbol.for('flagsSuspect')
const _setWorkspaces = Symbol.for('setWorkspaces')
const _updateNames = Symbol.for('updateNames')
const _resolvedAdd = Symbol.for('resolvedAdd')
const _usePackageLock = Symbol.for('usePackageLock')
const _rpcache = Symbol.for('realpathCache')
const _stcache = Symbol.for('statCache')

// used by Reify mixin
const _addNodeToTrashList = Symbol.for('addNodeToTrashList')

// Push items in, pop them sorted by depth and then path
// Sorts physically shallower deps up to the front of the queue, because
// they'll affect things deeper in, then alphabetical for consistency between
// installs
class DepsQueue {
  #deps = []
  #sorted = true

  get length () {
    return this.#deps.length
  }

  push (item) {
    if (!this.#deps.includes(item)) {
      this.#sorted = false
      this.#deps.push(item)
    }
  }

  pop () {
    if (!this.#sorted) {
      this.#deps.sort((a, b) => (a.depth - b.depth) || localeCompare(a.path, b.path))
      this.#sorted = true
    }
    return this.#deps.shift()
  }
}

module.exports = cls => class IdealTreeBuilder extends cls {
  #complete
  #currentDep = null
  #depsQueue = new DepsQueue()
  #depsSeen = new Set()
  #explicitRequests = new Set()
  #follow
  #installStrategy
  #linkNodes = new Set()
  #loadFailures = new Set()
  #manifests = new Map()
  #mutateTree = false
  // a map of each module in a peer set to the thing that depended on
  // that set of peers in the first place.  Use a WeakMap so that we
  // don't hold onto references for nodes that are garbage collected.
  #peerSetSource = new WeakMap()
  #preferDedupe = false
  #prune
  #strictPeerDeps
  #virtualRoots = new Map()

  constructor (options) {
    super(options)

    // normalize trailing slash
    const registry = options.registry || 'https://registry.npmjs.org'
    options.registry = this.registry = registry.replace(/\/+$/, '') + '/'

    const {
      follow = false,
      installStrategy = 'hoisted',
      idealTree = null,
      installLinks = false,
      legacyPeerDeps = false,
      packageLock = true,
      strictPeerDeps = false,
      workspaces,
      global,
    } = options

    this.#strictPeerDeps = !!strictPeerDeps

    this.idealTree = idealTree
    this.installLinks = installLinks
    this.legacyPeerDeps = legacyPeerDeps

    this[_usePackageLock] = packageLock
    this.#installStrategy = global ? 'shallow' : installStrategy
    this.#follow = !!follow

    if (workspaces?.length && global) {
      throw new Error('Cannot operate on workspaces in global mode')
    }

    this[_updateAll] = false
    this[_updateNames] = []
    this[_resolvedAdd] = []
  }

  get explicitRequests () {
    return new Set(this.#explicitRequests)
  }

  // public method
  async buildIdealTree (options = {}) {
    if (this.idealTree) {
      return this.idealTree
    }

    // allow the user to set reify options on the ctor as well.
    // XXX: deprecate separate reify() options object.
    options = { ...this.options, ...options }

    // an empty array or any falsey value is the same as null
    if (!options.add || options.add.length === 0) {
      options.add = null
    }
    if (!options.rm || options.rm.length === 0) {
      options.rm = null
    }

    const timeEnd = time.start('idealTree')

    if (!options.add && !options.rm && !options.update && this.options.global) {
      throw new Error('global requires add, rm, or update option')
    }

    // first get the virtual tree, if possible.  If there's a lockfile, then
    // that defines the ideal tree, unless the root package.json is not
    // satisfied by what the ideal tree provides.
    // from there, we start adding nodes to it to satisfy the deps requested
    // by the package.json in the root.

    this.#parseSettings(options)

    // start tracker block
    this.addTracker('idealTree')

    try {
      await this.#initTree()
      await this.#inflateAncientLockfile()
      await this.#applyUserRequests(options)
      await this.#buildDeps()
      await this.#fixDepFlags()
      await this.#pruneFailedOptional()
      await this.#checkEngineAndPlatform()
    } finally {
      timeEnd()
      this.finishTracker('idealTree')
    }

    return treeCheck(this.idealTree)
  }

  async #checkEngineAndPlatform () {
    const { engineStrict, npmVersion, nodeVersion, omit = [], cpu, os, libc } = this.options
    const omitSet = new Set(omit)

    for (const node of this.idealTree.inventory.values()) {
      if (!node.optional && !node.shouldOmit(omitSet)) {
        try {
          // if devEngines is present in the root node we ignore the engines check
          if (!(node.isRoot && node.package.devEngines)) {
            checkEngine(node.package, npmVersion, nodeVersion, this.options.force)
          }
        } catch (err) {
          if (engineStrict) {
            throw err
          }
          log.warn(err.code, err.message, {
            package: err.pkgid,
            required: err.required,
            current: err.current,
          })
        }
        checkPlatform(node.package, this.options.force)
      }
      if (node.optional && !node.inert) {
        // Mark any optional packages we can't install as inert.
        // We ignore the --force and --engine-strict flags.
        try {
          checkEngine(node.package, npmVersion, nodeVersion, false)
          checkPlatform(node.package, false, { cpu, os, libc })
        } catch (error) {
          const set = optionalSet(node)
          for (const node of set) {
            node.inert = true
          }
        }
      }
    }
  }

  #parseSettings (options) {
    const update = options.update === true ? { all: true }
      : Array.isArray(options.update) ? { names: options.update }
      : options.update || {}

    if (update.all || !Array.isArray(update.names)) {
      update.names = []
    }

    this.#complete = !!options.complete
    this.#preferDedupe = !!options.preferDedupe

    // validates list of update names, they must
    // be dep names only, no semver ranges are supported
    for (const name of update.names) {
      const spec = npa(name)
      const validationError =
        new TypeError(`Update arguments must only contain package names, eg:
    npm update ${spec.name}`)
      validationError.code = 'EUPDATEARGS'

      // If they gave us anything other than a bare package name
      if (spec.raw !== spec.name) {
        throw validationError
      }
    }
    this[_updateNames] = update.names

    this[_updateAll] = update.all
    // we prune by default unless explicitly set to boolean false
    this.#prune = options.prune !== false

    // set if we add anything, but also set here if we know we'll make
    // changes and thus have to maybe prune later.
    this.#mutateTree = !!(
      options.add ||
      options.rm ||
      update.all ||
      update.names.length
    )
  }

  // load the initial tree, either the virtualTree from a shrinkwrap,
  // or just the root node from a package.json
  async #initTree () {
    const timeEnd = time.start('idealTree:init')
    let root
    if (this.options.global) {
      root = await this.#globalRootNode()
    } else {
      try {
        const { content: pkg } = await PackageJson.normalize(this.path)
        root = await this.#rootNodeFromPackage(pkg)
      } catch (err) {
        if (err.code === 'EJSONPARSE') {
          throw err
        }
        root = await this.#rootNodeFromPackage({})
      }
    }
    return this[_setWorkspaces](root)
      // ok to not have a virtual tree.  probably initial install.
      // When updating all, we load the shrinkwrap, but don't bother
      // to build out the full virtual tree from it, since we'll be
      // reconstructing it anyway.
      .then(root => {
        if (this.options.global) {
          return root
        } else if (!this[_usePackageLock] || this[_updateAll]) {
          return Shrinkwrap.reset({
            path: this.path,
            lockfileVersion: this.options.lockfileVersion,
            resolveOptions: this.options,
          }).then(meta => Object.assign(root, { meta }))
        } else {
          return this.loadVirtual({ root })
            .then(tree => {
              this.#applyRootOverridesToWorkspaces(tree)
              return tree
            })
        }
      })

      // if we don't have a lockfile to go from, then start with the
      // actual tree, so we only make the minimum required changes.
      // don't do this for global installs or updates, because in those
      // cases we don't use a lockfile anyway.
      // Load on a new Arborist object, so the Nodes aren't the same,
      // or else it'll get super confusing when we change them!
      .then(async root => {
        if ((!this[_updateAll] && !this.options.global && !root.meta.loadedFromDisk) || (this.options.global && this[_updateNames].length)) {
          await new this.constructor(this.options).loadActual({ root })
          const tree = root.target
          // even though we didn't load it from a package-lock.json FILE,
          // we still loaded it "from disk", meaning we have to reset
          // dep flags before assuming that any mutations were reflected.
          if (tree.children.size) {
            root.meta.loadedFromDisk = true
            // set these so that we don't try to ancient lockfile reload it
            root.meta.originalLockfileVersion = root.meta.lockfileVersion = this.options.lockfileVersion || defaultLockfileVersion
          }
        }
        root.meta.inferFormattingOptions(root.package)
        return root
      })

      .then(tree => {
        // search the virtual tree for missing/invalid edges, if any are found add their source to
        // the depsQueue so that we'll fix it later
        depth({
          tree,
          getChildren: (node) => {
            const children = []
            for (const edge of node.edgesOut.values()) {
              children.push(edge.to)
            }
            return children
          },
          filter: node => node,
          visit: node => {
            for (const edge of node.edgesOut.values()) {
              if (!edge.to || !edge.valid) {
                this.#depsQueue.push(node)
                break // no need to continue the loop after the first hit
              }
            }
          },
        })
        // null the virtual tree, because we're about to hack away at it
        // if you want another one, load another copy.
        this.idealTree = tree
        this.virtualTree = null
        timeEnd()
        return tree
      })
  }

  async #globalRootNode () {
    const root = await this.#rootNodeFromPackage({ dependencies: {} })
    // this is a gross kludge to handle the fact that we don't save
    // metadata on the root node in global installs, because the "root"
    // node is something like /usr/local/lib.
    const meta = new Shrinkwrap({
      path: this.path,
      lockfileVersion: this.options.lockfileVersion,
      resolveOptions: this.options,
    })
    meta.reset()
    root.meta = meta
    return root
  }

  async #rootNodeFromPackage (pkg) {
    // if the path doesn't exist, then we explode at this point. Note that
    // this is not a problem for reify(), since it creates the root path
    // before ever loading trees.
    // TODO: make buildIdealTree() and loadActual handle a missing root path,
    // or a symlink to a missing target, and let reify() create it as needed.
    const real = await realpath(this.path, this[_rpcache], this[_stcache])
    const Cls = real === this.path ? Node : Link
    const root = new Cls({
      path: this.path,
      realpath: real,
      pkg,
      extraneous: false,
      dev: false,
      devOptional: false,
      peer: false,
      optional: false,
      global: this.options.global,
      installLinks: this.installLinks,
      legacyPeerDeps: this.legacyPeerDeps,
      loadOverrides: true,
    })
    if (root.isLink) {
      root.target = new Node({
        path: real,
        realpath: real,
        pkg,
        extraneous: false,
        dev: false,
        devOptional: false,
        peer: false,
        optional: false,
        global: this.options.global,
        installLinks: this.installLinks,
        legacyPeerDeps: this.legacyPeerDeps,
        root,
      })
    }
    return root
  }

  // process the add/rm requests by modifying the root node, and the
  // update.names request by queueing nodes dependent on those named.
  async #applyUserRequests (options) {
    const timeEnd = time.start('idealTree:userRequests')
    const tree = this.idealTree.target

    if (!this.options.workspaces.length) {
      await this.#applyUserRequestsToNode(tree, options)
    } else {
      const nodes = this.workspaceNodes(tree, this.options.workspaces)
      if (this.options.includeWorkspaceRoot) {
        nodes.push(tree)
      }
      const appliedRequests = nodes.map(
        node => this.#applyUserRequestsToNode(node, options)
      )
      await Promise.all(appliedRequests)
    }

    timeEnd()
  }

  async #applyUserRequestsToNode (tree, options) {
    // If we have a list of package names to update, and we know it's
    // going to update them wherever they are, add any paths into those
    // named nodes to the buildIdealTree queue.
    if (!this.options.global && this[_updateNames].length) {
      this.#queueNamedUpdates()
    }

    // global updates only update the globalTop nodes, but we need to know
    // that they're there, and not reinstall the world unnecessarily.
    const globalExplicitUpdateNames = []
    if (this.options.global && (this[_updateAll] || this[_updateNames].length)) {
      const nm = resolve(this.path, 'node_modules')
      const paths = await readdirScoped(nm).catch(() => [])
      for (const p of paths) {
        const name = p.replace(/\\/g, '/')
        const updateName = this[_updateNames].includes(name)
        if (this[_updateAll] || updateName) {
          if (updateName) {
            globalExplicitUpdateNames.push(name)
          }
          const dir = resolve(nm, name)
          const st = await lstat(dir)
            .catch(/* istanbul ignore next */ () => null)
          if (st && st.isSymbolicLink()) {
            const target = await readlink(dir)
            const real = resolve(dirname(dir), target)
            tree.package.dependencies[name] = `file:${real}`
          } else {
            tree.package.dependencies[name] = '*'
          }
        }
      }
    }

    if (this.auditReport && this.auditReport.size > 0) {
      await this.#queueVulnDependents(options)
    }

    const { add, rm } = options

    if (rm && rm.length) {
      addRmPkgDeps.rm(tree.package, rm)
      for (const name of rm) {
        this.#explicitRequests.add({ from: tree, name, action: 'DELETE' })
      }
    }

    if (add && add.length) {
      await this.#add(tree, options)
    }

    // triggers a refresh of all edgesOut.  this has to be done BEFORE
    // adding the edges to explicitRequests, because the package setter
    // resets all edgesOut.
    if (add && add.length || rm && rm.length || this.options.global) {
      tree.package = tree.package
    }

    for (const spec of this[_resolvedAdd]) {
      if (spec.tree === tree) {
        this.#explicitRequests.add(tree.edgesOut.get(spec.name))
      }
    }
    for (const name of globalExplicitUpdateNames) {
      this.#explicitRequests.add(tree.edgesOut.get(name))
    }

    this.#depsQueue.push(tree)
  }

  // This returns a promise because we might not have the name yet, and need to
  // call pacote.manifest to find the name.
  async #add (tree, { add, saveType = null, saveBundle = false }) {
    // If we have a link it will need to be added relative to the target's path
    const path = tree.target.path

    // get the name for each of the specs in the list.
    // ie, doing `foo@bar` we just return foo but if it's a url or git, we
    // don't know the name until we fetch it and look in its manifest.
    await Promise.all(add.map(async rawSpec => {
      // We do NOT provide the path to npa here, because user-additions need to
      // be resolved relative to the tree being added to.
      let spec = npa(rawSpec)

      // if it's just @'' then we reload whatever's there, or get latest
      // if it's an explicit tag, we need to install that specific tag version
      const isTag = spec.rawSpec && spec.type === 'tag'

      // look up the names of file/directory/git specs
      if (!spec.name || isTag) {
        const mani = await pacote.manifest(spec, { ...this.options })
        if (isTag) {
          // translate tag to a version
          spec = npa(`${mani.name}@${mani.version}`)
        }
        spec.name = mani.name
      }

      const { name } = spec
      if (spec.type === 'file') {
        spec = npa(`file:${relpath(path, spec.fetchSpec)}`, path)
        spec.name = name
      } else if (spec.type === 'directory') {
        try {
          const real = await realpath(spec.fetchSpec, this[_rpcache], this[_stcache])
          spec = npa(`file:${relpath(path, real)}`, path)
          spec.name = name
        } catch {
          // TODO: create synthetic test case to simulate realpath failure
        }
      }
      spec.tree = tree
      this[_resolvedAdd].push(spec)
    }))

    // now this._resolvedAdd is a list of spec objects with names.
    // find a home for each of them!
    addRmPkgDeps.add({
      pkg: tree.package,
      add: this[_resolvedAdd],
      saveBundle,
      saveType,
    })
  }

  // TODO: provide a way to fix bundled deps by exposing metadata about
  // what's in the bundle at each published manifest.  Without that, we
  // can't possibly fix bundled deps without breaking a ton of other stuff,
  // and leaving the user subject to getting it overwritten later anyway.
  async #queueVulnDependents (options) {
    for (const vuln of this.auditReport.values()) {
      for (const node of vuln.nodes) {
        const bundler = node.getBundler()

        // XXX this belongs in the audit report itself, not here.
        // We shouldn't even get these things here, and they shouldn't
        // be printed by npm-audit-report as if they can be fixed, because
        // they can't.
        if (bundler) {
          log.warn(`audit fix ${node.name}@${node.version}`,
            `${node.location}\nis a bundled dependency of\n${
            bundler.name}@${bundler.version} at ${bundler.location}\n` +
            'It cannot be fixed automatically.\n' +
            `Check for updates to the ${bundler.name} package.`)
          continue
        }

        for (const edge of node.edgesIn) {
          this.addTracker('idealTree', edge.from.name, edge.from.location)
          this.#depsQueue.push(edge.from)
        }
      }
    }

    // note any that can't be fixed at the root level without --force
    // if there's a fix, we use that.  otherwise, the user has to remove it,
    // find a different thing, fix the upstream, etc.
    //
    // XXX: how to handle top nodes that aren't the root?  Maybe the report
    // just tells the user to cd into that directory and fix it?
    if (this.options.force && this.auditReport && this.auditReport.topVulns.size) {
      options.add = options.add || []
      options.rm = options.rm || []
      const nodesTouched = new Set()
      for (const [name, topVuln] of this.auditReport.topVulns.entries()) {
        const {
          simpleRange,
          topNodes,
          fixAvailable,
        } = topVuln
        for (const node of topNodes) {
          if (!node.isProjectRoot && !node.isWorkspace) {
            // not something we're going to fix, sorry.  have to cd into
            // that directory and fix it yourself.
            log.warn('audit', 'Manual fix required in linked project ' +
              `at ./${node.location} for ${name}@${simpleRange}.\n` +
              `'cd ./${node.location}' and run 'npm audit' for details.`)
            continue
          }

          if (!fixAvailable) {
            log.warn('audit', `No fix available for ${name}@${simpleRange}`)
            continue
          }

          // name may be different if parent fixes the dep
          // see Vuln fixAvailable setter
          const { isSemVerMajor, version, name: fixName } = fixAvailable
          const breakingMessage = isSemVerMajor
            ? 'a SemVer major change'
            : 'outside your stated dependency range'
          log.warn('audit', `Updating ${fixName} to ${version}, ` +
            `which is ${breakingMessage}.`)

          await this.#add(node, { add: [`${fixName}@${version}`] })
          nodesTouched.add(node)
        }
      }
      for (const node of nodesTouched) {
        node.package = node.package
      }
    }
  }

  #avoidRange (name) {
    if (!this.auditReport) {
      return null
    }
    const vuln = this.auditReport.get(name)
    if (!vuln) {
      return null
    }
    return vuln.range
  }

  #queueNamedUpdates () {
    // ignore top nodes, since they are not loaded the same way, and
    // probably have their own project associated with them.

    // for every node with one of the names on the list, we add its
    // dependents to the queue to be evaluated.  in buildDepStep,
    // anything on the update names list will get refreshed, even if
    // it isn't a problem.

    // XXX this could be faster by doing a series of inventory.query('name')
    // calls rather than walking over everything in the tree.
    for (const node of this.idealTree.inventory.values()) {
      // XXX add any invalid edgesOut to the queue
      if (this[_updateNames].includes(node.name) &&
        !node.isTop && !node.inDepBundle && !node.inShrinkwrap) {
        for (const edge of node.edgesIn) {
          this.addTracker('idealTree', edge.from.name, edge.from.location)
          this.#depsQueue.push(edge.from)
        }
      }
    }
  }

  async #inflateAncientLockfile () {
    const { meta, inventory } = this.idealTree
    const ancient = meta.ancientLockfile
    const old = meta.loadedFromDisk && !(meta.originalLockfileVersion >= 2)

    if (inventory.size === 0 || !ancient && !old) {
      return
    }

    // if the lockfile is from node v5 or earlier, then we'll have to reload
    // all the manifests of everything we encounter.  this is costly, but at
    // least it's just a one-time hit.
    const timeEnd = time.start('idealTree:inflate')

    // don't warn if we're not gonna actually write it back anyway.
    const heading = ancient ? 'ancient lockfile' : 'old lockfile'
    if (ancient || !this.options.lockfileVersion ||
        this.options.lockfileVersion >= defaultLockfileVersion) {
      log.warn(heading,
        `
The ${meta.type} file was created with an old version of npm,
so supplemental metadata must be fetched from the registry.

This is a one-time fix-up, please be patient...
`)
    }

    this.addTracker('idealTree:inflate')
    const queue = []
    for (const node of inventory.values()) {
      if (node.isProjectRoot) {
        continue
      }

      // if the node's location isn't within node_modules then this is actually
      // a link target, so skip it. the link node itself will be queued later.
      if (!node.location.startsWith('node_modules')) {
        continue
      }

      queue.push(async () => {
        log.silly('inflate', node.location)
        const { resolved, version, path, name, location, integrity } = node
        // don't try to hit the registry for linked deps
        const useResolved = resolved && (
          !version || resolved.startsWith('file:')
        )
        const id = useResolved ? resolved : version
        const spec = npa.resolve(name, id, dirname(path))
        const t = `idealTree:inflate:${location}`
        this.addTracker(t)
        try {
          const mani = await pacote.manifest(spec, {
            ...this.options,
            resolved: resolved,
            integrity: integrity,
            fullMetadata: false,
          })
          node.package = { ...mani, _id: `${mani.name}@${mani.version}` }
        } catch (er) {
          const warning = `Could not fetch metadata for ${name}@${id}`
          log.warn(heading, warning, er)
        }
        this.finishTracker(t)
      })
    }
    await promiseCallLimit(queue)

    // have to re-calc dep flags, because the nodes don't have edges
    // until their packages get assigned, so everything looks extraneous
    calcDepFlags(this.idealTree)

    // yes, yes, this isn't the "original" version, but now that it's been
    // upgraded, we need to make sure we don't do the work to upgrade it
    // again, since it's now as new as can be.
    if (!this.options.lockfileVersion && !meta.hiddenLockfile) {
      meta.originalLockfileVersion = defaultLockfileVersion
    }
    this.finishTracker('idealTree:inflate')
    timeEnd()
  }

  // at this point we have a virtual tree with the actual root node's
  // package deps, which may be partly or entirely incomplete, invalid
  // or extraneous.
  #buildDeps () {
    const timeEnd = time.start('idealTree:buildDeps')
    const tree = this.idealTree.target
    tree.assertRootOverrides()
    this.#depsQueue.push(tree)
    // XXX also push anything that depends on a node with a name
    // in the override list
    log.silly('idealTree', 'buildDeps')
    this.addTracker('idealTree', tree.name, '')
    return this.#buildDepStep().then(timeEnd)
  }

  async #buildDepStep () {
    // removes tracker of previous dependency in the queue
    if (this.#currentDep) {
      const { location, name } = this.#currentDep
      time.end(`idealTree:${location || '#root'}`)
      this.finishTracker('idealTree', name, location)
      this.#currentDep = null
    }

    if (!this.#depsQueue.length) {
      return this.#resolveLinks()
    }

    const node = this.#depsQueue.pop()
    const bd = node.package.bundleDependencies
    const hasBundle = bd && Array.isArray(bd) && bd.length
    const { hasShrinkwrap } = node

    // if the node was already visited, or has since been removed from the
    // tree, skip over it and process the rest of the queue.  If a node has
    // a shrinkwrap, also skip it, because it's going to get its deps
    // satisfied by whatever's in that file anyway.
    if (this.#depsSeen.has(node) ||
        node.root !== this.idealTree ||
        hasShrinkwrap && !this.#complete) {
      return this.#buildDepStep()
    }

    this.#depsSeen.add(node)
    this.#currentDep = node
    time.start(`idealTree:${node.location || '#root'}`)

    // if we're loading a _complete_ ideal tree, for a --package-lock-only
    // installation for example, we have to crack open the tarball and
    // look inside if it has bundle deps or shrinkwraps.  note that this is
    // not necessary during a reification, because we just update the
    // ideal tree by reading bundles/shrinkwraps in place.
    // Don't bother if the node is from the actual tree and hasn't
    // been resolved, because we can't fetch it anyway, could be anything!
    const crackOpen = this.#complete &&
      node !== this.idealTree &&
      node.resolved &&
      (hasBundle || hasShrinkwrap) &&
      !node.inert
    if (crackOpen) {
      const Arborist = this.constructor
      const opt = { ...this.options }
      await cacache.tmp.withTmp(this.cache, opt, async path => {
        await pacote.extract(node.resolved, path, {
          ...opt,
          Arborist,
          resolved: node.resolved,
          integrity: node.integrity,
        })

        if (hasShrinkwrap) {
          await new Arborist({ ...this.options, path })
            .loadVirtual({ root: node })
        }

        if (hasBundle) {
          await new Arborist({ ...this.options, path })
            .loadActual({ root: node, ignoreMissing: true })
        }
      })
    }

    // if any deps are missing or invalid, then we fetch the manifest for
    // the thing we want, and build a new dep node from that.
    // Then, find the ideal placement for that node.  The ideal placement
    // searches from the node's deps (or parent deps in the case of non-root
    // peer deps), and walks up the tree until it finds the highest spot
    // where it doesn't cause any conflicts.
    //
    // A conflict can be:
    // - A node by that name already exists at that location.
    // - The parent has a peer dep on that name
    // - One of the node's peer deps conflicts at that location, unless the
    //   peer dep is met by a node at that location, which is fine.
    //
    // If we create a new node, then build its ideal deps as well.
    //
    // Note: this is the same "maximally naive" deduping tree-building
    // algorithm that npm has used since v3.  In a case like this:
    //
    // root -> (a@1, b@1||2)
    // a -> (b@1)
    //
    // You'll end up with a tree like this:
    //
    // root
    // +-- a@1
    // |   +-- b@1
    // +-- b@2
    //
    // rather than this, more deduped, but just as correct tree:
    //
    // root
    // +-- a@1
    // +-- b@1
    //
    // Another way to look at it is that this algorithm favors getting higher
    // version deps at higher levels in the tree, even if that reduces
    // potential deduplication.
    //
    // Set `preferDedupe: true` in the options to replace the shallower
    // dep if allowed.

    const tasks = []
    const peerSource = this.#peerSetSource.get(node) || node
    for (const edge of this.#problemEdges(node)) {
      if (edge.peerConflicted) {
        continue
      }

      // peerSetSource is only relevant when we have a peerEntryEdge
      // otherwise we're setting regular non-peer deps as if they have
      // a virtual root of whatever brought in THIS node.
      // so we VR the node itself if the edge is not a peer
      const source = edge.peer ? peerSource : node

      const virtualRoot = this.#virtualRoot(source, true)
      // reuse virtual root if we already have one, but don't
      // try to do the override ahead of time, since we MAY be able
      // to create a more correct tree than the virtual root could.
      const vrEdge = virtualRoot && virtualRoot.edgesOut.get(edge.name)
      const vrDep = vrEdge && vrEdge.valid && vrEdge.to
      // only re-use the virtualRoot if it's a peer edge we're placing.
      // otherwise, we end up in situations where we override peer deps that
      // we could have otherwise found homes for.  Eg:
      // xy -> (x, y)
      // x -> PEER(z@1)
      // y -> PEER(z@2)
      // If xy is a dependency, we can resolve this like:
      // project
      // +-- xy
      // |   +-- y
      // |   +-- z@2
      // +-- x
      // +-- z@1
      // But if x and y are loaded in the same virtual root, then they will
      // be forced to agree on a version of z.
      const required = new Set([edge.from])
      const parent = edge.peer ? virtualRoot : null
      const dep = vrDep && vrDep.satisfies(edge) ? vrDep
        : await this.#nodeFromEdge(edge, parent, null, required)

      /* istanbul ignore next */
      debug(() => {
        if (!dep) {
          throw new Error('no dep??')
        }
      })

      tasks.push({ edge, dep })
    }

    const placeDeps = tasks.sort((a, b) => localeCompare(a.edge.name, b.edge.name))

    const promises = []
    for (const { edge, dep } of placeDeps) {
      const pd = new PlaceDep({
        edge,
        dep,

        auditReport: this.auditReport,
        explicitRequest: this.#explicitRequests.has(edge),
        force: this.options.force,
        installLinks: this.installLinks,
        installStrategy: this.#installStrategy,
        legacyPeerDeps: this.legacyPeerDeps,
        preferDedupe: this.#preferDedupe,
        strictPeerDeps: this.#strictPeerDeps,
        updateNames: this[_updateNames],
      })
      // placing a dep is actually a tree of placing the dep itself
      // and all of its peer group that aren't already met by the tree
      depth({
        tree: pd,
        getChildren: pd => pd.children,
        visit: pd => {
          const { placed, edge, canPlace: cpd } = pd
          // if we didn't place anything, nothing to do here
          if (!placed) {
            return
          }

          // we placed something, that means we changed the tree
          if (placed.errors.length) {
            this.#loadFailures.add(placed)
          }
          this.#mutateTree = true
          if (cpd.canPlaceSelf === OK) {
            for (const edgeIn of placed.edgesIn) {
              if (edgeIn === edge) {
                continue
              }
              const { from, valid, peerConflicted } = edgeIn
              if (!peerConflicted && !valid && !this.#depsSeen.has(from)) {
                this.addTracker('idealTree', from.name, from.location)
                this.#depsQueue.push(edgeIn.from)
              }
            }
          } else {
            /* istanbul ignore else - should be only OK or REPLACE here */
            if (cpd.canPlaceSelf === REPLACE) {
              // this may also create some invalid edges, for example if we're
              // intentionally causing something to get nested which was
              // previously placed in this location.
              for (const edgeIn of placed.edgesIn) {
                if (edgeIn === edge) {
                  continue
                }

                const { valid, peerConflicted } = edgeIn
                if (!valid && !peerConflicted) {
                  // if it's already been visited, we have to re-visit
                  // otherwise, just enqueue normally.
                  this.#depsSeen.delete(edgeIn.from)
                  this.#depsQueue.push(edgeIn.from)
                }
              }
            }
          }

          /* istanbul ignore if - should be impossible */
          if (cpd.canPlaceSelf === CONFLICT) {
            debug(() => {
              const er = new Error('placed with canPlaceSelf=CONFLICT')
              throw Object.assign(er, { placeDep: pd })
            })
            return
          }

          // lastly, also check for the missing deps of the node we placed,
          // and any holes created by pruning out conflicted peer sets.
          this.#depsQueue.push(placed)
          for (const dep of pd.needEvaluation) {
            this.#depsSeen.delete(dep)
            this.#depsQueue.push(dep)
          }

          // pre-fetch any problem edges, since we'll need these soon
          // if it fails at this point, though, don't worry because it
          // may well be an optional dep that has gone missing.  it'll
          // fail later anyway.
          for (const e of this.#problemEdges(placed)) {
            // XXX This is somehow load bearing.  This makes tests that print
            // the ideal tree of a tree with tarball dependencies fail. This
            // can't be changed or removed till we figure out why
            // The test is named "tarball deps with transitive tarball deps"
            promises.push(() =>
              this.#fetchManifest(npa.resolve(e.name, e.spec, fromPath(placed, e)))
                .catch(() => null)
            )
          }
        },
      })
    }

    for (const { to } of node.edgesOut.values()) {
      if (to && to.isLink && to.target) {
        this.#linkNodes.add(to)
      }
    }

    await promiseCallLimit(promises)
    return this.#buildDepStep()
  }

  // loads a node from an edge, and then loads its peer deps (and their
  // peer deps, on down the line) into a virtual root parent.
  async #nodeFromEdge (edge, parent_, secondEdge, required) {
    // create a virtual root node with the same deps as the node that
    // is requesting this one, so that we can get all the peer deps in
    // a context where they're likely to be resolvable.
    // Note that the virtual root will also have virtual copies of the
    // targets of any child Links, so that they resolve appropriately.
    const parent = parent_ || this.#virtualRoot(edge.from)

    const spec = npa.resolve(edge.name, edge.spec, edge.from.path)
    const first = await this.#nodeFromSpec(edge.name, spec, parent, edge)

    // we might have a case where the parent has a peer dependency on
    // `foo@*` which resolves to v2, but another dep in the set has a
    // peerDependency on `foo@1`.  In that case, if we force it to be v2,
    // we're unnecessarily triggering an ERESOLVE.
    // If we have a second edge to worry about, and it's not satisfied
    // by the first node, try a second and see if that satisfies the
    // original edge here.
    const spec2 = secondEdge && npa.resolve(
      edge.name,
      secondEdge.spec,
      secondEdge.from.path
    )
    const second = secondEdge && !secondEdge.valid
      ? await this.#nodeFromSpec(edge.name, spec2, parent, secondEdge)
      : null

    // pick the second one if they're both happy with that; otherwise, first
    const node = second && edge.valid ? second : first
    // ensure the one we want is the one that's placed
    node.parent = parent

    if (required.has(edge.from) && edge.type !== 'peerOptional' ||
        secondEdge && (
          required.has(secondEdge.from) && secondEdge.type !== 'peerOptional')) {
      required.add(node)
    }

    // keep track of the thing that caused this node to be included.
    const src = parent.sourceReference
    this.#peerSetSource.set(node, src)

    // do not load the peers along with the set if this is a global top pkg
    // otherwise we'll be tempted to put peers as other top-level installed
    // things, potentially clobbering what's there already, which is not
    // what we want.  the missing edges will be picked up on the next pass.
    if (this.options.global && edge.from.isProjectRoot) {
      return node
    }

    // otherwise, we have to make sure that our peers can go along with us.
    return this.#loadPeerSet(node, required)
  }

  #virtualRoot (node, reuse = false) {
    if (reuse && this.#virtualRoots.has(node)) {
      return this.#virtualRoots.get(node)
    }

    const vr = new Node({
      path: node.realpath,
      sourceReference: node,
      installLinks: this.installLinks,
      legacyPeerDeps: this.legacyPeerDeps,
      overrides: node.overrides,
    })

    // also need to set up any targets from any link deps, so that
    // they are properly reflected in the virtual environment
    for (const child of node.children.values()) {
      if (child.isLink) {
        new Node({
          path: child.realpath,
          sourceReference: child.target,
          root: vr,
        })
      }
    }

    this.#virtualRoots.set(node, vr)
    return vr
  }

  #problemEdges (node) {
    // skip over any bundled deps, they're not our problem.
    // Note that this WILL fetch bundled meta-deps which are also dependencies
    // but not listed as bundled deps.  When reifying, we first unpack any
    // nodes that have bundleDependencies, then do a loadActual on them, move
    // the nodes into the ideal tree, and then prune.  So, fetching those
    // possibly-bundled meta-deps at this point doesn't cause any worse
    // problems than a few unnecessary packument fetches.

    // also skip over any nodes in the tree that failed to load, since those
    // will crash the install later on anyway.
    const bd = node.isProjectRoot || node.isWorkspace ? null
      : node.package.bundleDependencies
    const bundled = new Set(bd || [])

    const problems = []
    for (const edge of node.edgesOut.values()) {
      // If it's included in a bundle, we take whatever is specified.
      if (bundled.has(edge.name)) {
        continue
      }

      // If it's already been logged as a load failure, skip it.
      if (edge.to && this.#loadFailures.has(edge.to)) {
        continue
      }

      // If it's shrinkwrapped, we use what the shrinkwap wants.
      if (edge.to && edge.to.inShrinkwrap) {
        continue
      }

      // If the edge has no destination, that's a problem, unless
      // if it's peerOptional and not explicitly requested.
      if (!edge.to) {
        if (edge.type !== 'peerOptional' ||
          this.#explicitRequests.has(edge)) {
          problems.push(edge)
        }
        continue
      }

      // If the edge has an error, there's a problem.
      if (!edge.valid) {
        problems.push(edge)
        continue
      }

      // If the edge is a workspace, and it's valid, leave it alone
      if (edge.to.isWorkspace) {
        continue
      }

      // user explicitly asked to update this package by name, problem
      if (this[_updateNames].includes(edge.name)) {
        problems.push(edge)
        continue
      }

      // fixing a security vulnerability with this package, problem
      if (this.auditReport && this.auditReport.isVulnerable(edge.to)) {
        problems.push(edge)
        continue
      }

      // user has explicitly asked to install this package, problem
      if (this.#explicitRequests.has(edge)) {
        problems.push(edge)
        continue
      }
    }
    return problems
  }

  async #fetchManifest (spec) {
    const options = {
      ...this.options,
      avoid: this.#avoidRange(spec.name),
      fullMetadata: true,
    }
    // get the intended spec and stored metadata from yarn.lock file,
    // if available and valid.
    spec = this.idealTree.meta.checkYarnLock(spec, options)

    if (this.#manifests.has(spec.raw)) {
      return this.#manifests.get(spec.raw)
    } else {
      log.silly('fetch manifest', spec.raw.replace(spec.rawSpec, redact(spec.rawSpec)))
      const mani = await pacote.manifest(spec, options)
      this.#manifests.set(spec.raw, mani)
      return mani
    }
  }

  #nodeFromSpec (name, spec, parent, edge) {
    // pacote will slap integrity on its options, so we have to clone
    // the object so it doesn't get mutated.
    // Don't bother to load the manifest for link deps, because the target
    // might be within another package that doesn't exist yet.
    const { installLinks, legacyPeerDeps } = this
    const isWorkspace = this.idealTree.workspaces && this.idealTree.workspaces.has(spec.name)

    // spec is a directory, link it if:
    // - it's a workspace, OR
    // - it's a project-internal file: dependency (always linked), OR
    // - it's external and installLinks is false
    // TODO post arborist refactor, will need to check for installStrategy=linked
    let isProjectInternalFileSpec = false
    if (edge?.rawSpec.startsWith('file:../') || edge?.rawSpec.startsWith('file:./')) {
      const targetPath = resolve(parent.realpath, edge.rawSpec.slice(5))
      const resolvedProjectRoot = resolve(this.idealTree.realpath)
      // Check if the target is within the project root
      isProjectInternalFileSpec = targetPath.startsWith(resolvedProjectRoot + sep) || targetPath === resolvedProjectRoot
    }

    // When using --install-links, we need to handle transitive file dependencies specially
    // If the parent was installed (not linked) due to --install-links, and this is a file: dep, we should also install it rather than link it
    const parentWasInstalled = parent && !parent.isLink && parent.resolved?.startsWith('file:')
    const isTransitiveFileDep = spec.type === 'directory' && parentWasInstalled && installLinks

    // Decide whether to link or copy the dependency
    const shouldLink = (isWorkspace || isProjectInternalFileSpec || !installLinks) && !isTransitiveFileDep
    if (spec.type === 'directory' && shouldLink) {
      return this.#linkFromSpec(name, spec, parent, edge)
    }

    // if the spec matches a workspace name, then see if the workspace node will satisfy the edge. if it does, we return the workspace node to make sure it takes priority.
    if (isWorkspace) {
      const existingNode = this.idealTree.edgesOut.get(spec.name).to
      if (existingNode && existingNode.isWorkspace && existingNode.satisfies(edge)) {
        return existingNode
      }
    }

    // For file: dependencies that we're installing (not linking), ensure proper resolution
    if (isTransitiveFileDep && edge) {
      // For transitive file deps, resolve relative to the parent's original source location
      const parentOriginalPath = parent.resolved.slice(5) // Remove 'file:' prefix
      const relativePath = edge.rawSpec.slice(5) // Remove 'file:' prefix
      const absolutePath = resolve(parentOriginalPath, relativePath)
      spec = npa.resolve(name, `file:${absolutePath}`)
    }

    // spec isn't a directory, and either isn't a workspace or the workspace we have
    // doesn't satisfy the edge. try to fetch a manifest and build a node from that.
    return this.#fetchManifest(spec)
      .then(pkg => new Node({ name, pkg, parent, installLinks, legacyPeerDeps }), error => {
        error.requiredBy = edge.from.location || '.'

        // failed to load the spec, either because of enotarget or
        // fetch failure of some other sort.  save it so we can verify
        // later that it's optional; otherwise, the error is fatal.
        const n = new Node({
          name,
          parent,
          error,
          installLinks,
          legacyPeerDeps,
        })
        this.#loadFailures.add(n)
        return n
      })
  }

  async #linkFromSpec (name, spec, parent) {
    const realpath = spec.fetchSpec
    const { installLinks, legacyPeerDeps } = this
    const { content: pkg } = await PackageJson.normalize(realpath).catch(() => {
      return { content: {} }
    })
    const link = new Link({ name, parent, realpath, pkg, installLinks, legacyPeerDeps })
    this.#linkNodes.add(link)
    return link
  }

  // load all peer deps and meta-peer deps into the node's parent
  // At the end of this, the node's peer-type outward edges are all
  // resolved, and so are all of theirs, but other dep types are not.
  // We prefer to get peer deps that meet the requiring node's dependency,
  // if possible, since that almost certainly works (since that package was
  // developed with this set of deps) and will typically be more restrictive.
  // Note that the peers in the set can conflict either with each other,
  // or with a direct dependency from the virtual root parent!  In strict
  // mode, this is always an error.  In force mode, it never is, and we
  // prefer the parent's non-peer dep over a peer dep, or the version that
  // gets placed first.  In non-strict mode, we behave strictly if the
  // virtual root is based on the root project, and allow non-peer parent
  // deps to override, but throw if no preference can be determined.
  async #loadPeerSet (node, required) {
    const peerEdges = [...node.edgesOut.values()]
      // we typically only install non-optional peers, but we have to
      // factor them into the peerSet so that we can avoid conflicts
      .filter(e => e.peer && !(e.valid && e.to))
      .sort(({ name: a }, { name: b }) => localeCompare(a, b))

    for (const edge of peerEdges) {
      // node.parent gets mutated during loop execution due to recursive #nodeFromEdge calls.
      // When a compatible peer is found (e.g. a@1.1.0 replaces a@1.2.0), the original node loses its parent.
      // if node is detached/removed from the tree, or has no parent, so no need to check remaining edgesOut for that node.
      if (!node.parent) {
        break
      }
      // already placed this one, and we're happy with it.
      if (edge.valid && edge.to) {
        continue
      }

      const parentEdge = node.parent.edgesOut.get(edge.name)
      const { isProjectRoot, isWorkspace } = node.parent.sourceReference
      const isMine = isProjectRoot || isWorkspace
      const conflictOK = this.options.force || !isMine && !this.#strictPeerDeps

      if (!edge.to) {
        if (!parentEdge) {
          // easy, just put the thing there
          await this.#nodeFromEdge(edge, node.parent, null, required)
          continue
        } else {
          // if the parent's edge is very broad like >=1, and the edge in
          // question is something like 1.x, then we want to get a 1.x, not
          // a 2.x.  pass along the child edge as an advisory guideline.
          // if the parent edge doesn't satisfy the child edge, and the
          // child edge doesn't satisfy the parent edge, then we have
          // a conflict.  this is always a problem in strict mode, never
          // in force mode, and a problem in non-strict mode if this isn't
          // on behalf of our project.  in all such cases, we warn at least.
          const dep = await this.#nodeFromEdge(
            parentEdge,
            node.parent,
            edge,
            required
          )

          // hooray! that worked!
          if (edge.valid) {
            continue
          }

          // allow it.  either we're overriding, or it's not something
          // that will be installed by default anyway, and we'll fail when
          // we get to the point where we need to, if we need to.
          if (conflictOK || !required.has(dep)) {
            edge.peerConflicted = true
            continue
          }

          // problem
          this.#failPeerConflict(edge, parentEdge)
        }
      }

      // There is something present already, and we're not happy about it
      // See if the thing we WOULD be happy with is also going to satisfy
      // the other dependents on the current node.
      const current = edge.to
      const dep = await this.#nodeFromEdge(edge, null, null, required)
      if (dep.canReplace(current)) {
        await this.#nodeFromEdge(edge, node.parent, null, required)
        continue
      }

      // at this point we know that there is a dep there, and
      // we don't like it.  always fail strictly, always allow forcibly or
      // in non-strict mode if it's not our fault.  don't warn here, because
      // we are going to warn again when we place the deps, if we end up
      // overriding for something else.  If the thing that has this dep
      // isn't also required, then there's a good chance we won't need it,
      // so allow it for now and let it conflict if it turns out to actually
      // be necessary for the installation.
      if (conflictOK || !required.has(edge.from)) {
        continue
      }

      // ok, it's the root, or we're in unforced strict mode, so this is bad
      this.#failPeerConflict(edge, parentEdge)
    }
    return node
  }

  #failPeerConflict (edge, currentEdge) {
    const expl = this.#explainPeerConflict(edge, currentEdge)
    throw Object.assign(new Error('unable to resolve dependency tree'), expl)
  }

  #explainPeerConflict (edge, currentEdge) {
    const node = edge.from
    const curNode = node.resolve(edge.name)
    const current = curNode.explain()
    return {
      code: 'ERESOLVE',
      current,
      // it SHOULD be impossible to get here without a current node in place,
      // but this at least gives us something report on when bugs creep into
      // the tree handling logic.
      currentEdge: currentEdge ? currentEdge.explain() : null,
      edge: edge.explain(),
      strictPeerDeps: this.#strictPeerDeps,
      force: this.options.force,
    }
  }

  // go through all the links in the this.#linkNodes set
  // for each one:
  // - if outside the root, ignore it, assume it's fine, it's not our problem
  // - if a node in the tree already, assign the target to that node.
  // - if a path under an existing node, then assign that as the fsParent,
  //   and add it to the _depsQueue
  //
  // call buildDepStep if anything was added to the queue; otherwise, we're done
  #resolveLinks () {
    for (const link of this.#linkNodes) {
      this.#linkNodes.delete(link)

      // link we never ended up placing, skip it
      if (link.root !== this.idealTree) {
        continue
      }

      const tree = this.idealTree.target
      const external = !link.target.isDescendantOf(tree)

      // outside the root, somebody else's problem, ignore it
      if (external && !this.#follow) {
        continue
      }

      // didn't find a parent for it or it has not been seen yet
      // so go ahead and process it.
      const unseenLink = (link.target.parent || link.target.fsParent) &&
        !this.#depsSeen.has(link.target)

      if (this.#follow &&
          !link.target.parent &&
          !link.target.fsParent ||
          unseenLink) {
        this.addTracker('idealTree', link.target.name, link.target.location)
        this.#depsQueue.push(link.target)
      }
    }

    if (this.#depsQueue.length) {
      return this.#buildDepStep()
    }
  }

  #fixDepFlags () {
    const timeEnd = time.start('idealTree:fixDepFlags')
    const metaFromDisk = this.idealTree.meta.loadedFromDisk
    const flagsSuspect = this[_flagsSuspect]
    const mutateTree = this.#mutateTree
    // if the options set prune:false, then we don't prune, but we still
    // mark the extraneous items in the tree if we modified it at all.
    // If we did no modifications, we just iterate over the extraneous nodes.
    // if we started with an empty tree, then the dep flags are already
    // all set to true, and there can be nothing extraneous, so there's
    // nothing to prune, because we built it from scratch.  if we didn't
    // add or remove anything, then also nothing to do.
    if (metaFromDisk && mutateTree) {
      resetDepFlags(this.idealTree)
    }

    // update all the dev/optional/etc flags in the tree
    // either we started with a fresh tree, or we
    // reset all the flags to find the extraneous nodes.
    //
    // if we started from a blank slate, or changed something, then
    // the dep flags will be all set to true.
    if (!metaFromDisk || mutateTree) {
      calcDepFlags(this.idealTree)
    } else {
      // otherwise just unset all the flags on the root node
      // since they will sometimes have the default value
      this.idealTree.extraneous = false
      this.idealTree.dev = false
      this.idealTree.optional = false
      this.idealTree.devOptional = false
      this.idealTree.peer = false
    }

    // at this point, any node marked as extraneous should be pruned.
    // if we started from a shrinkwrap, and then added/removed something,
    // then the tree is suspect.  Prune what is marked as extraneous.
    // otherwise, don't bother.
    const needPrune = metaFromDisk && (mutateTree || flagsSuspect)
    if (this.#prune && needPrune) {
      this.#idealTreePrune()
    }

    timeEnd()
  }

  #applyRootOverridesToWorkspaces (tree) {
    const rootOverrides = tree.root.package.overrides || {}

    for (const node of tree.root.inventory.values()) {
      if (!node.isWorkspace) {
        continue
      }

      for (const depName of Object.keys(rootOverrides)) {
        const edge = node.edgesOut.get(depName)
        const rootNode = tree.root.children.get(depName)

        // safely skip if either edge or rootNode doesn't exist yet
        if (!edge || !rootNode) {
          continue
        }

        const resolvedRootVersion = rootNode.package.version
        if (!semver.satisfies(resolvedRootVersion, edge.spec)) {
          edge.detach()
          node.children.delete(depName)
        }
      }
    }
  }

  #idealTreePrune () {
    for (const node of this.idealTree.inventory.values()) {
      // optional peer dependencies are meant to be added to the tree
      // through an explicit required dependency (most commonly in the
      // root package.json), at which point they won't be optional so
      // any dependencies still marked as both optional and peer at
      // this point can be pruned as a special kind of extraneous
      if (node.extraneous || (node.peer && node.optional)) {
        node.parent = null
      }
    }
  }

  #pruneFailedOptional () {
    for (const node of this.#loadFailures) {
      if (!node.optional) {
        throw node.errors[0]
      }

      const set = optionalSet(node)
      for (const node of set) {
        node.inert = true
      }
    }
  }

  async prune (options = {}) {
    // allow the user to set options on the ctor as well.
    // XXX: deprecate separate method options objects.
    options = { ...this.options, ...options }

    await this.buildIdealTree(options)

    this.#idealTreePrune()

    if (!this.options.workspacesEnabled) {
      const excludeNodes = this.excludeWorkspacesDependencySet(this.idealTree)
      for (const node of this.idealTree.inventory.values()) {
        if (
          node.parent !== null
          && !node.isProjectRoot
          && !excludeNodes.has(node)
          && !node.inert
        ) {
          this[_addNodeToTrashList](node)
        }
      }
    }

    return this.reify(options)
  }
}

577 verbose npm  v11.6.2
578 verbose exit 1
579 verbose code 1
580 error A complete log of this run can be found in: C:\Projekte\pzw-mkp\.npx\_logs\2026-05-20T14_15_09_031Z-debug-0.log
